{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"utils/","title":"Utilities","text":""},{"location":"utils/#add-location","title":"Add Location","text":"<p>Business logic that connects between the Details, Database, and LLM to add a location to the system</p> <p>This module contains the following functions: - <code>add(location)</code> - Adds the location to the database</p>"},{"location":"utils/#app.utils.add_location.add","title":"<code>add(location_name)</code>","text":"<p>Checks the database if the location_name already exists, if it doesn't it uses wiki &amp; an LLM to get landmarks &amp; the lat/long of the location, and stores them in the database.</p> <p>Parameters:</p> Name Type Description Default <code>location_name</code> <code>str</code> <p>Name of the place (e.g. 'Melbourne')</p> required <p>Returns:</p> Type Description <code>int</code> <p>The ID of the location in the database</p> Usage <p>add('Melbourne') 1</p> Source code in <code>app/utils/add_location.py</code> <pre><code>def add(location_name: str) -&gt; int:\n    \"\"\"\n    Checks the database if the location_name already exists, if it doesn't\n    it uses wiki &amp; an LLM to get landmarks &amp; the lat/long of the location,\n    and stores them in the database.\n\n    Args:\n        location_name (str): Name of the place (e.g. 'Melbourne')\n\n    Returns:\n        The ID of the location in the database\n\n    Raises:\n        ValueError if the location is already in the database.\n\n    Usage:\n        &gt;&gt;&gt; add('Melbourne')\n        1\n    \"\"\"\n    location_name = location_name.strip()\n    existing_location = Location_Metadata.select().where(\n        Location_Metadata.name_lower == location_name.lower()\n    )\n    # assert existing_location, ValueError(\"Location already exists\")\n    details, lat, long = location.get_details(location_name)\n    landmarks = llm.get_landmarks(location_name, details)\n    # lat, long = llm.get_lat_long(details)\n    loc = Location_Metadata.create(\n        name_lower=location_name.lower().strip(),\n        name=location_name,\n        wiki_data=details,\n        landmarks=landmarks,\n        lat=lat,\n        long=long,\n    )\n    return loc.id\n</code></pre>"},{"location":"weather/","title":"Weather","text":""},{"location":"weather/#app.weather.weather--weatherapi-interface","title":"WeatherAPI Interface","text":"<p>This library (<code>app/weather/weather.py</code>) interfaces with WeatherAPI and collects either the current or the forecast weather conditions, including:     - Queried location's match details (i.e. the matches name, region, country, lat/long, timezone)     - Condition text     - Temperature     - Wind     - Precipitation     - UV</p>"},{"location":"weather/#app.weather.weather.get_current","title":"<code>get_current(client, location)</code>  <code>async</code>","text":"<p>Get the current weather information for a location</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>AsyncClient</code> <p>A web client to perform the get request</p> required <code>location</code> <code>str</code> <p>Query param, could be city name, lat/long, IP, etc</p> required Usage <p>asyncio.run(get_current(httpx.AsyncClient(), 'Melbourne')) Current_Weather(...)</p> Source code in <code>app/weather/weather.py</code> <pre><code>async def get_current(\n    client: httpx.AsyncClient, location: str\n) -&gt; weather_schema.Current_Weather:\n    \"\"\"\n    Get the current weather information for a location\n\n    Args:\n        client (httpx.AsyncClient): A web client to perform the get request\n        location (str): Query param, could be city name, lat/long, IP, etc\n\n    Usage:\n        &gt;&gt;&gt; asyncio.run(get_current(httpx.AsyncClient(), 'Melbourne'))\n        Current_Weather(...)\n    \"\"\"\n    response = await client.get(\n        url=CONFIG.WEATHER_CURRENT_URL,\n        params={\"q\": location, \"key\": CONFIG.WEATHER_API_KEY},\n    )\n    ret_val = weather_schema.Current_Weather.model_validate_json(response.content)\n    return ret_val\n</code></pre>"},{"location":"weather/#app.weather.weather.get_forecast","title":"<code>get_forecast(client, location)</code>  <code>async</code>","text":"<p>Get the forecast weather information for a location</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>AsyncClient</code> <p>A web client to perform the get request</p> required <code>location</code> <code>str</code> <p>Query param, could be city name, lat/long, IP, etc</p> required Usage <p>asyncio.run(get_forecast(httpx.AsyncClient(), 'Melbourne')) Forecast_Weather(...)</p> Source code in <code>app/weather/weather.py</code> <pre><code>async def get_forecast(\n    client: httpx.AsyncClient, location: str\n) -&gt; weather_schema.Forecast_Weather:\n    \"\"\"\n    Get the _forecast_ weather information for a location\n\n    Args:\n        client (httpx.AsyncClient): A web client to perform the get request\n        location (str): Query param, could be city name, lat/long, IP, etc\n\n    Usage:\n        &gt;&gt;&gt; asyncio.run(get_forecast(httpx.AsyncClient(), 'Melbourne'))\n        Forecast_Weather(...)\n    \"\"\"\n    response = await client.get(\n        url=CONFIG.WEATHER_FORECAST_URL,\n        params={\"q\": location, \"key\": CONFIG.WEATHER_API_KEY, \"days\": 3},\n    )\n    ret_val = weather_schema.Forecast_Weather.model_validate_json(response.content)\n    return ret_val\n</code></pre>"},{"location":"weather/#app.weather.weather.get_httpx_client","title":"<code>get_httpx_client(app)</code>  <code>async</code>","text":"<p>Provides an httpx.AsyncClient instance to the FastAPI Application for the apps lifespan. Note that it uses the yield statement, so it closes the client when the FastAPI app closes.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>FastAPI</code> <p>Adds the httpx client</p> required Source code in <code>app/weather/weather.py</code> <pre><code>@asynccontextmanager\nasync def get_httpx_client(app: FastAPI):\n    \"\"\"\n    Provides an httpx.AsyncClient instance to the FastAPI Application for\n    the apps lifespan.  \n    Note that it uses the yield statement, so it closes the client when the\n    FastAPI app closes.\n\n    Args:\n        app (FastAPI): Adds the httpx client \n    \"\"\"\n    async with httpx.AsyncClient() as client:\n        app.httpx_client = client\n        yield\n</code></pre>"},{"location":"weather_schema/","title":"Weather Schema","text":""},{"location":"weather_schema/#app.schemas.weather_schema--weather-schema","title":"Weather Schema","text":"<p>This library (<code>app/schemas/weather_schema.py</code>) describes the information returned by the WeatherAPI API, as used by the weather library.</p> <p>The top level responses used are Current_Weather and Forecast_Weather</p>"},{"location":"weather_schema/#app.schemas.weather_schema.Astro","title":"<code>Astro</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Astrological (i.e. sun &amp; moon) information, as returned by Weather API</p> <p>Parameters:</p> Name Type Description Default <code>sunrise</code> <code>str</code> <p>e.g. \"06:02 AM\"</p> required <code>sunset</code> <code>str</code> <p>e.g. \"08:45 PM\"</p> required <code>moonrise</code> <code>str</code> <p>e.g. \"08:00 PM\"</p> required <code>moonset</code> <code>str</code> <p>e.g. \"04:00 AM\"</p> required <code>moon_phase</code> <code>str</code> <p>e.g. \"Waxing Gibbous\"</p> required <code>moon_illumination</code> <code>int</code> <p>e.g. 97</p> required <code>is_moon_up</code> <code>bool</code> <p>e.g. 1</p> required <code>is_sun_up</code> <code>bool</code> <p>e.g. 0</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Astro(BaseModel):\n    \"\"\"\n    Astrological (i.e. sun &amp; moon) information, as returned by Weather API\n\n    Args:\n        sunrise (str): e.g. \"06:02 AM\"\n        sunset (str): e.g. \"08:45 PM\"\n        moonrise (str): e.g. \"08:00 PM\"\n        moonset (str): e.g. \"04:00 AM\"\n        moon_phase (str): e.g. \"Waxing Gibbous\"\n        moon_illumination (int): e.g. 97\n        is_moon_up (bool): e.g. 1\n        is_sun_up (bool): e.g. 0\n    \"\"\"\n\n    sunrise: str\n    sunset: str\n    moonrise: str\n    moonset: str\n    moon_phase: str\n    moon_illumination: int\n    is_moon_up: bool\n    is_sun_up: bool\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Condition","title":"<code>Condition</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Weather Conditions at the location, as returned by Weather API</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>e.g. \"Sunny\"</p> required <code>icon</code> <code>str</code> <p>e.g. \"//cdn.weatherapi.com/weather/64x64/day/113.png\"</p> required <code>code</code> <code>int</code> <p>e.g. 1000</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Condition(BaseModel):\n    \"\"\"\n    Weather Conditions at the location, as returned by Weather API\n\n    Args:\n        text (str): e.g. \"Sunny\"\n        icon (str): e.g. \"//cdn.weatherapi.com/weather/64x64/day/113.png\"\n        code (int): e.g. 1000\n    \"\"\"\n\n    text: str\n    icon: str\n    code: int\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Current","title":"<code>Current</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Current weather (including 'Condition') at the location, as returned by Weather API</p> <p>Parameters:</p> Name Type Description Default <code>last_updated_epoch</code> <code>int</code> <p>e.g. 1767329100</p> required <code>last_updated</code> <code>str</code> <p>e.g. \"2026-01-02 15:45\"</p> required <code>temp_c</code> <code>float</code> <p>e.g. 26.1</p> required <code>temp_f</code> <code>float</code> <p>e.g. 79</p> required <code>is_day</code> <code>int</code> <p>e.g. 1</p> required <code>condition</code> <code>Condition</code> <p>See above</p> required <code>wind_mph</code> <code>float</code> <p>e.g. 9.4</p> required <code>wind_kph</code> <code>float</code> <p>e.g. 15.1</p> required <code>wind_degree</code> <code>int</code> <p>e.g. 204</p> required <code>wind_dir</code> <code>str</code> <p>e.g. \"SSW\", \"WSW\"</p> required <code>pressure_mb</code> <code>int</code> <p>e.g. 1013</p> required <code>pressure_in</code> <code>float</code> <p>e.g. 29.91</p> required <code>precip_mm</code> <code>int</code> <p>e.g. 0</p> required <code>precip_in</code> <code>int</code> <p>e.g. 0</p> required <code>humidity</code> <code>int</code> <p>e.g. 39</p> required <code>cloud</code> <code>int</code> <p>e.g. 0</p> required <code>feelslike_c</code> <code>float</code> <p>e.g. 26.1</p> required <code>feelslike_f</code> <code>float</code> <p>e.g. 79</p> required <code>windchill_c</code> <code>float</code> <p>e.g. 24.3</p> required <code>windchill_f</code> <code>float</code> <p>e.g. 75.7</p> required <code>heatindex_c</code> <code>float</code> <p>e.g. 24.9</p> required <code>heatindex_f</code> <code>float</code> <p>e.g. 76.8</p> required <code>dewpoint_c</code> <code>float</code> <p>e.g. 9.7</p> required <code>dewpoint_f</code> <code>float</code> <p>e.g. 49.4</p> required <code>vis_km</code> <code>float</code> <p>e.g. 10</p> required <code>vis_miles</code> <code>float</code> <p>e.g. 6</p> required <code>uv</code> <code>float</code> <p>e.g. 10.6</p> required <code>gust_mph</code> <code>float</code> <p>e.g. 10.8</p> required <code>gust_kph</code> <code>float</code> <p>e.g. 17.4</p> required <code>short_rad</code> <code>float</code> <p>e.g. 1014.66</p> required <code>diff_rad</code> <code>float</code> <p>e.g. 138.6</p> required <code>dni</code> <code>float</code> <p>Direct Normal Irradiance (intensity of sunlight) e.g. 0</p> required <code>gti</code> <code>float</code> <p>Global Tilted Irradiance (total sunlight for solar panels) e.g. 145.36</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Current(BaseModel):\n    \"\"\"\n    Current weather (including 'Condition') at the location, as returned by Weather API\n\n    Args:\n        last_updated_epoch (int): e.g. 1767329100\n        last_updated (str): e.g. \"2026-01-02 15:45\"\n        temp_c (float): e.g. 26.1\n        temp_f (float): e.g. 79\n        is_day (int): e.g. 1\n        condition (Condition): See above\n        wind_mph (float): e.g. 9.4\n        wind_kph (float): e.g. 15.1\n        wind_degree (int): e.g. 204\n        wind_dir (str): e.g. \"SSW\", \"WSW\"\n        pressure_mb (int): e.g. 1013\n        pressure_in (float): e.g. 29.91\n        precip_mm (int): e.g. 0\n        precip_in (int): e.g. 0\n        humidity (int): e.g. 39\n        cloud (int): e.g. 0\n        feelslike_c (float): e.g. 26.1\n        feelslike_f (float): e.g. 79\n        windchill_c (float): e.g. 24.3\n        windchill_f (float): e.g. 75.7\n        heatindex_c (float): e.g. 24.9\n        heatindex_f (float): e.g. 76.8\n        dewpoint_c (float): e.g. 9.7\n        dewpoint_f (float): e.g. 49.4\n        vis_km (float): e.g. 10\n        vis_miles (float): e.g. 6\n        uv (float): e.g. 10.6\n        gust_mph (float): e.g. 10.8\n        gust_kph (float): e.g. 17.4\n        short_rad (float): e.g. 1014.66\n        diff_rad (float): e.g. 138.6\n        dni (float): Direct Normal Irradiance (intensity of sunlight) e.g. 0\n        gti (float): Global Tilted Irradiance (total sunlight for solar panels) e.g. 145.36\n    \"\"\"\n\n    last_updated_epoch: int\n    last_updated: str\n    temp_c: float\n    temp_f: float\n    is_day: int\n    condition: Condition\n    wind_mph: float\n    wind_kph: float\n    wind_degree: int\n    wind_dir: str\n    pressure_mb: int\n    pressure_in: float\n    precip_mm: int\n    precip_in: int\n    humidity: int\n    cloud: int\n    feelslike_c: float\n    feelslike_f: float\n    windchill_c: float\n    windchill_f: float\n    heatindex_c: float\n    heatindex_f: float\n    dewpoint_c: float\n    dewpoint_f: float\n    vis_km: float\n    vis_miles: float\n    uv: float\n    gust_mph: float\n    gust_kph: float\n    short_rad: float\n    diff_rad: float\n    dni: float\n    gti: float\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Current_Weather","title":"<code>Current_Weather</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The object returned by <code>http://api.weatherapi.com/v1/current.json</code> Contains information about the matched location, and the current weather conditions</p> <p>Parameters:</p> Name Type Description Default <code>location</code> <code>Location</code> <p>name, state, country, lat/long, timezone</p> required <code>current</code> <code>Current</code> <p>temp, wind, uv, gust, etc.</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Current_Weather(BaseModel):\n    \"\"\"\n    The object returned by `http://api.weatherapi.com/v1/current.json`\n    Contains information about the matched location, and the current weather conditions\n\n    Args:\n        location (Location): name, state, country, lat/long, timezone\n        current (Current): temp, wind, uv, gust, etc.\n    \"\"\"\n\n    location: Location\n    current: Current\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Day","title":"<code>Day</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Daily overview (i.e. max, min, average, total) of the location, as returned by Weather API</p> <p>Parameters:</p> Name Type Description Default <code>maxtemp_c</code> <code>float</code> <p>e.g. 24.2</p> required <code>maxtemp_f</code> <code>float</code> <p>e.g. 75.6</p> required <code>mintemp_c</code> <code>float</code> <p>e.g. 12.7</p> required <code>mintemp_f</code> <code>float</code> <p>e.g. 54.8</p> required <code>avgtemp_c</code> <code>float</code> <p>e.g. 18.5</p> required <code>avgtemp_f</code> <code>float</code> <p>e.g. 65.4</p> required <code>maxwind_mph</code> <code>float</code> <p>e.g. 10.7</p> required <code>maxwind_kph</code> <code>float</code> <p>e.g. 17.3</p> required <code>totalprecip_mm</code> <code>float</code> <p>e.g. 0</p> required <code>totalprecip_in</code> <code>float</code> <p>e.g. 0</p> required <code>totalsnow_cm</code> <code>float</code> <p>e.g. 0</p> required <code>avgvis_km</code> <code>float</code> <p>e.g. 10</p> required <code>avgvis_miles</code> <code>float</code> <p>e.g. 6</p> required <code>avghumidity</code> <code>int</code> <p>Average humidity as percentage. e.g. 60</p> required <code>daily_will_it_rain</code> <code>bool</code> <p>e.g. 0</p> required <code>daily_chance_of_rain</code> <code>int</code> <p>Chance of rain as percentage. e.g. 0</p> required <code>daily_will_it_snow</code> <code>bool</code> <p>e.g. 0</p> required <code>daily_chance_of_snow</code> <code>int</code> <p>Chance of snow as percentage. e.g. 0</p> required <code>condition</code> <code>Condition</code> <p>See above</p> required <code>uv</code> <code>float</code> <p>e.g. 3.3</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Day(BaseModel):\n    \"\"\"\n    Daily overview (i.e. max, min, average, total) of the location, as returned by Weather API\n\n    Args:\n        maxtemp_c (float): e.g. 24.2\n        maxtemp_f (float): e.g. 75.6\n        mintemp_c (float): e.g. 12.7\n        mintemp_f (float): e.g. 54.8\n        avgtemp_c (float): e.g. 18.5\n        avgtemp_f (float): e.g. 65.4\n        maxwind_mph (float): e.g. 10.7\n        maxwind_kph (float): e.g. 17.3\n        totalprecip_mm (float): e.g. 0\n        totalprecip_in (float): e.g. 0\n        totalsnow_cm (float): e.g. 0\n        avgvis_km (float): e.g. 10\n        avgvis_miles (float): e.g. 6\n        avghumidity (int): Average humidity as percentage. e.g. 60\n        daily_will_it_rain (bool): e.g. 0\n        daily_chance_of_rain (int): Chance of rain as percentage. e.g. 0\n        daily_will_it_snow (bool): e.g. 0\n        daily_chance_of_snow (int): Chance of snow as percentage. e.g. 0\n        condition (Condition): See above\n        uv (float): e.g. 3.3\n    \"\"\"\n\n    maxtemp_c: float\n    maxtemp_f: float\n    mintemp_c: float\n    mintemp_f: float\n    avgtemp_c: float\n    avgtemp_f: float\n    maxwind_mph: float\n    maxwind_kph: float\n    totalprecip_mm: float\n    totalprecip_in: float\n    totalsnow_cm: float\n    avgvis_km: float\n    avgvis_miles: float\n    avghumidity: int\n    daily_will_it_rain: bool\n    daily_chance_of_rain: int\n    daily_will_it_snow: bool\n    daily_chance_of_snow: int\n    condition: Condition\n    uv: float\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Forecast","title":"<code>Forecast</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The forecast conditions for the following days</p> <p>Parameters:</p> Name Type Description Default <code>forecastday (List[Forecast_Day]) </code> <p>A list of each days total information</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Forecast(BaseModel):\n    \"\"\"\n    The forecast conditions for the following days\n\n    Args:\n        forecastday (List[Forecast_Day]) : A list of each days total information\n    \"\"\"\n    forecastday: List[Forecast_Day]\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Forecast_Day","title":"<code>Forecast_Day</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The total information returned by Weather API for a day.</p> <p>Parameters:</p> Name Type Description Default <code>date</code> <code>str</code> <p>e.g. \"2026-01-02\"</p> required <code>date_epoch</code> <code>int</code> <p>e.g. 1767312000</p> required <code>day</code> <code>Day</code> <p>See above</p> required <code>astro</code> <code>Astro</code> <p>See above</p> required <code>hour</code> <code>List[Hour]</code> <p>See above</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Forecast_Day(BaseModel):\n    \"\"\"\n    The total information returned by Weather API for a day.\n\n    Args:\n        date (str): e.g. \"2026-01-02\"\n        date_epoch (int): e.g. 1767312000\n        day (Day): See above\n        astro (Astro): See above\n        hour (List[Hour]): See above\n    \"\"\"\n\n    date: str\n    date_epoch: int\n    day: Day\n    astro: Astro\n    hour: List[Hour]\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Forecast_Weather","title":"<code>Forecast_Weather</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The object returned by <code>http://api.weatherapi.com/v1/forecast.json</code> Contains information about the matched location, and the current weather conditions</p> <p>Parameters:</p> Name Type Description Default <code>location</code> <code>Location</code> <p>name, state, country, lat/long, timezone</p> required <code>current</code> <code>Current</code> <p>temp, wind, uv, gust, etc.</p> required <code>forecast</code> <code>Forecast</code> <p>daily/hourly temp, wind, uv, gust, etc.</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Forecast_Weather(BaseModel):\n    \"\"\"\n    The object returned by `http://api.weatherapi.com/v1/forecast.json`\n    Contains information about the matched location, and the current weather conditions\n\n    Args:\n        location (Location): name, state, country, lat/long, timezone\n        current (Current): temp, wind, uv, gust, etc.\n        forecast (Forecast): daily/hourly temp, wind, uv, gust, etc.\n    \"\"\"\n\n    location: Location\n    current: Current\n    forecast: Forecast\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Hour","title":"<code>Hour</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Information to cover a single hour of weather, as returned by Weather API. Note <code>Hours</code> is a list of this <code>Hour</code> class</p> <p>Parameters:</p> Name Type Description Default <code>time_epoch</code> <code>int</code> <p>e.g. 1767272400</p> required <code>time</code> <code>str</code> <p>Start time for the hour, e.g. \"2026-01-02 00:00\"</p> required <code>temp_c</code> <code>float</code> <p>e.g. 15.4</p> required <code>temp_f</code> <code>float</code> <p>e.g. 59.7</p> required <code>is_day</code> <code>int</code> <p>e.g. 0</p> required <code>condition</code> <code>Condition</code> <p>See above</p> required <code>wind_mph</code> <code>float</code> <p>e.g. 9.2</p> required <code>wind_kph</code> <code>float</code> <p>e.g. 14.8</p> required <code>wind_degree</code> <code>float</code> <p>e.g. 173</p> required <code>wind_dir</code> <code>str</code> <p>e.g. \"S\"</p> required <code>pressure_mb</code> <code>float</code> <p>e.g. 1016</p> required <code>pressure_in</code> <code>float</code> <p>e.g. 29.99</p> required <code>precip_mm</code> <code>float</code> <p>e.g. 0</p> required <code>precip_in</code> <code>float</code> <p>e.g. 0</p> required <code>snow_cm</code> <code>float</code> <p>e.g. 0</p> required <code>humidity</code> <code>float</code> <p>e.g. 70</p> required <code>cloud</code> <code>float</code> <p>e.g. 0</p> required <code>feelslike_c</code> <code>float</code> <p>e.g. 15.4</p> required <code>feelslike_f</code> <code>float</code> <p>e.g. 59.6</p> required <code>windchill_c</code> <code>float</code> <p>e.g. 15.4</p> required <code>windchill_f</code> <code>float</code> <p>e.g. 59.6</p> required <code>heatindex_c</code> <code>float</code> <p>e.g. 15.4</p> required <code>heatindex_f</code> <code>float</code> <p>e.g. 59.6</p> required <code>dewpoint_c</code> <code>float</code> <p>e.g. 9.9</p> required <code>dewpoint_f</code> <code>float</code> <p>e.g. 49.8</p> required <code>will_it_rain</code> <code>bool</code> <p>e.g. 0</p> required <code>chance_of_rain</code> <code>int</code> <p>Chance of rain as percentage. e.g. 0</p> required <code>will_it_snow</code> <code>bool</code> <p>e.g. 0</p> required <code>chance_of_snow</code> <code>int</code> <p>Chance of snow as percentage. e.g. 0</p> required <code>vis_km</code> <code>float</code> <p>e.g. 10</p> required <code>vis_miles</code> <code>float</code> <p>e.g. 6</p> required <code>gust_mph</code> <code>float</code> <p>e.g. 13.5</p> required <code>gust_kph</code> <code>float</code> <p>e.g. 21.8</p> required <code>uv</code> <code>float</code> <p>e.g. 0</p> required <code>short_rad</code> <code>float</code> <p>e.g. 0</p> required <code>diff_rad</code> <code>float</code> <p>e.g. 0</p> required <code>dni</code> <code>float</code> <p>Direct Normal Irradiance (intensity of sunlight) e.g. 0</p> required <code>gti</code> <code>float</code> <p>Global Tilted Irradiance (total sunlight for solar panels) e.g. 145.36</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Hour(BaseModel):\n    \"\"\"\n    Information to cover a single hour of weather, as returned by Weather API.\n    Note `Hours` is a list of this `Hour` class\n\n    Args:\n        time_epoch (int): e.g. 1767272400\n        time (str): Start time for the hour, e.g. \"2026-01-02 00:00\"\n        temp_c (float): e.g. 15.4\n        temp_f (float): e.g. 59.7\n        is_day (int): e.g. 0\n        condition (Condition): See above\n        wind_mph (float): e.g. 9.2\n        wind_kph (float): e.g. 14.8\n        wind_degree (float): e.g. 173\n        wind_dir (str): e.g. \"S\"\n        pressure_mb (float): e.g. 1016\n        pressure_in (float): e.g. 29.99\n        precip_mm (float): e.g. 0\n        precip_in (float): e.g. 0\n        snow_cm (float): e.g. 0\n        humidity (float): e.g. 70\n        cloud (float): e.g. 0\n        feelslike_c (float): e.g. 15.4\n        feelslike_f (float): e.g. 59.6\n        windchill_c (float): e.g. 15.4\n        windchill_f (float): e.g. 59.6\n        heatindex_c (float): e.g. 15.4\n        heatindex_f (float): e.g. 59.6\n        dewpoint_c (float): e.g. 9.9\n        dewpoint_f (float): e.g. 49.8\n        will_it_rain (bool): e.g. 0\n        chance_of_rain (int): Chance of rain as percentage. e.g. 0\n        will_it_snow (bool): e.g. 0\n        chance_of_snow (int): Chance of snow as percentage. e.g. 0\n        vis_km (float): e.g. 10\n        vis_miles (float): e.g. 6\n        gust_mph (float): e.g. 13.5\n        gust_kph (float): e.g. 21.8\n        uv (float): e.g. 0\n        short_rad (float): e.g. 0\n        diff_rad (float): e.g. 0\n        dni (float): Direct Normal Irradiance (intensity of sunlight) e.g. 0\n        gti (float): Global Tilted Irradiance (total sunlight for solar panels) e.g. 145.36\n    \"\"\"\n\n    time_epoch: int\n    time: str\n    temp_c: float\n    temp_f: float\n    is_day: int\n    condition: Condition\n    wind_mph: float\n    wind_kph: float\n    wind_degree: float\n    wind_dir: str\n    pressure_mb: float\n    pressure_in: float\n    precip_mm: float\n    precip_in: float\n    snow_cm: float\n    humidity: float\n    cloud: float\n    feelslike_c: float\n    feelslike_f: float\n    windchill_c: float\n    windchill_f: float\n    heatindex_c: float\n    heatindex_f: float\n    dewpoint_c: float\n    dewpoint_f: float\n    will_it_rain: bool\n    chance_of_rain: int\n    will_it_snow: bool\n    chance_of_snow: int\n    vis_km: float\n    vis_miles: float\n    gust_mph: float\n    gust_kph: float\n    uv: float\n    short_rad: float\n    diff_rad: float\n    dni: float\n    gti: float\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Hours","title":"<code>Hours</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A list of each 'Hour' object to describe a day. Note that hours that have already passed are blank</p> Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Hours(BaseModel):\n    \"\"\"\n    A list of each 'Hour' object to describe a day.  \n    Note that hours that have already passed are blank\n    \"\"\"\n    List[Hour]\n</code></pre>"},{"location":"weather_schema/#app.schemas.weather_schema.Location","title":"<code>Location</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Location as matched by Weather API to the location name given</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>e.g. \"Melbourne\"</p> required <code>region</code> <code>str</code> <p>e.g. \"Victoria\"</p> required <code>country</code> <code>str</code> <p>e.g. \"Australia\"</p> required <code>lat</code> <code>float</code> <p>e.g. -37.8167</p> required <code>lon</code> <code>float</code> <p>e.g. 144.9667</p> required <code>tz_id</code> <code>str</code> <p>e.g. \"Australia/Melbourne\"</p> required <code>localtime_epoch</code> <code>int</code> <p>e.g. 1767329771</p> required <code>localtime</code> <code>str</code> <p>e.g. \"2026-01-02 15:56\"</p> required Source code in <code>app/schemas/weather_schema.py</code> <pre><code>class Location(BaseModel):\n    \"\"\"\n    Location as matched by Weather API to the location name given\n\n    Args:\n        name (str): e.g. \"Melbourne\"\n        region (str): e.g. \"Victoria\"\n        country (str): e.g. \"Australia\"\n        lat (float): e.g. -37.8167\n        lon (float): e.g. 144.9667\n        tz_id (str): e.g. \"Australia/Melbourne\"\n        localtime_epoch (int): e.g. 1767329771\n        localtime (str): e.g. \"2026-01-02 15:56\"\n    \"\"\"\n\n    name: str\n    region: str\n    country: str\n    lat: float\n    lon: float\n    tz_id: str\n    localtime_epoch: int\n    localtime: str\n</code></pre>"}]}